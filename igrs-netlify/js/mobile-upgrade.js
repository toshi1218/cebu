/* /js/mobile-upgrade.js - DISABLED */
(() => {
  // This file is disabled to prevent DOM manipulation issues
  // Navigation is now handled by CSS only
  console.log('mobile-upgrade.js is disabled');
})();
